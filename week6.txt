import psycopg2

con = psycopg2.connect(
  database="postgres", 
  user="postgres", 
  password="Kaliakakya", 
  host="127.0.0.1", 
  port="5432"
)

print("Database opened successfully")cur = con.cursor()  
cur.execute('''CREATE TABLE STUDENT  
     (ADMISSION INT PRIMARY KEY NOT NULL,
     NAME TEXT NOT NULL,
     AGE INT NOT NULL,
     COURSE CHAR(50),
     DEPARTMENT CHAR(50));''')

print("Table created successfully")
con.commit()  
con.close()


Then we get 
Database opened successfully  
Table created successfully 

import psycopg2

con = psycopg2.connect(
  database="postgres", 
  user="postgres", 
  password="mekaru", 
  host="127.0.0.1", 
  port="5432"
)

print("Database opened successfully")
cur = con.cursor()
cur.execute(
  "INSERT INTO STUDENT (ADMISSION,NAME,AGE,COURSE,DEPARTMENT) VALUES (3420, 'Aruzhan', 19, 'Cybersecurity', 'ICT')"
)

con.commit()  
print("Record inserted successfully")  

con.close()


                               data extraction

import psycopg2

con = psycopg2.connect(
  database="postgres", 
  user="postgres", 
  password="Kaliakakya", 
  host="127.0.0.1", 
  port="5432"
) 
 
print("Database opened successfully")cur = con.cursor()  
cur.execute("SELECT admission, name, age, course, department from STUDENT")
  
rows = cur.fetchall()
for row in rows:  
   print("ADMISSION =", row[0])
   print("NAME =", row[1])
   print("AGE =", row[2])
   print("COURSE =", row[3])
   print("DEPARTMENT =", row[4], "\n")

print("Operation done successfully")  
con.close()  


and we get
Database opened successfully  
ADMISSION = 3420  
NAME = Aruzhan  
AGE = 18  
COURSE = Cybersecurity 
DEPARTMENT = Python

ADMISSION = 3419  
NAME = Akbota  
AGE = 17  
COURSE = Cybersecurity
DEPARTMENT = Python

ADMISSION = 3421  
NAME = Akku 
AGE = 17  
COURSE = Cybersecurity  
DEPARTMENT = Python

ADMISSION = 3422  
NAME = Nursultan 
AGE = 19  
COURSE = Soft engineer 
DEPARTMENT = Engineering

ADMISSION = 3423  
NAME = Alina  
AGE = 18  
COURSE = Information Technology  
DEPARTMENT = ICT

Operation done successfully  
